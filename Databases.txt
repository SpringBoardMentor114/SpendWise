SQL> create table userDetails(
  2  userId number PRIMARY KEY,
  3  Firstname varchar2(50) NOT NULL,
  4  LastName varchar2(50) ,
  5  Email varchar2(50),
  6  Password varchar2(50) NOT NULL);

Table created.

SQL> create table Expense(
  2  ExpenseId number,
  3  userId number,
  4  ExpenseDate date,
  5  Amount decimal,
  6  CategoryId number PRIMARY KEY,
  7  Description varchar2(100),
  8  Foreign key (userId) references userDetails(userId));

Table created.


SQL> create table Category(
  2  CategoryId number,
  3  CategoryName varchar2(50),
  4  Foreign key (CategoryId) references Expense(CategoryId));

Table created.

SQL> insert into userDetails values (101,'A','Rahul','abc@gmail.com',123456);

1 row created.